[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "kws_framework"
version = "0.1.0"
description = "Few-shot Keyword Spotting Research Framework"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
